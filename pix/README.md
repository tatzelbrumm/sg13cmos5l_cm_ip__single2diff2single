# `pix/`

Images for the notes in `sudelbuecher/`. Empty as of 2026-08-31 — the sessions
logged so far produced no figures. The one screenshot in the transcript
(gitk) was pasted by the user and not saved here.

Not the place for design renders: those are generated artifacts and belong in
`render/img/`, produced by `make render-gds`.
